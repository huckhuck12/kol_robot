## 实现兼容本地轮询方案

### 1. 修改主程序入口（src/index.js）
- 添加两种运行模式支持：
  - 单次执行模式：用于GitHub Actions短轮询（保持现有功能）
  - 本地持续轮询模式：用于本地运行
- 通过命令行参数`--single`区分模式
- 使用`setInterval`实现本地定时轮询

### 2. 更新GitHub Actions工作流
- 修改run命令，添加`--single`参数，明确使用单次执行模式
- 保持其他配置不变

### 3. 保留原有功能
- 所有现有功能保持不变
- API请求、信号处理、钉钉推送逻辑保持一致
- 本地存储功能保持不变

### 4. 优化用户体验
- 添加清晰的运行模式提示
- 支持优雅退出
- 详细的日志输出

### 5. 测试方案
- 本地运行：执行`node src/index.js`启动持续轮询
- GitHub Actions：保持原有触发机制
- 验证两种模式均可正常工作